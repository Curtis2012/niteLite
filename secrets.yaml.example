# Copy this file's values into your real secrets.yaml (already gitignored,
# or add it to .gitignore) - do NOT commit real credentials.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

mqtt_broker: "YOUR_MQTT_BROKER_HOST"

ota_password: "REPLACE_WITH_OTA_PASSWORD"
